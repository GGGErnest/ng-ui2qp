import { Directive } from '@angular/core';

@Directive({
  // tslint:disable-next-line:directive-selector
  selector: '[topBarLeftTemplate]',
})
export class TopBarLeftTemplateDirective {

  constructor() {
  }
}
