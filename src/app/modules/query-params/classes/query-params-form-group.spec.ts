import { QueryParamsFormGroup } from './query-params-form-group';

describe('QueryParamsFormGroup', () => {
  it('should create an instance', () => {
    expect(new QueryParamsFormGroup()).toBeTruthy();
  });
});
